type: object
properties:
  id:
    type: string
    description: Mã id.
    readOnly: true
  internal_product_id:
    type: string
    description: Trường `internal_product_id`.
    readOnly: true
  provider_product_id:
    type: string
    description: Trường `provider_product_id`.
    readOnly: true
  provider_id:
    type: string
    description: Trường `provider_id`.
    readOnly: true
  deleted_at:
    type: string
    description: Ngày xóa.
  deleted_by:
    type: string
    description: Bị xóa bởi.
  reason:
    type: string
    description: Lý do.
description: Schema `DeleteInternalProductMappingData`.
