type: object
properties:
  id:
    type: string
    description: Mã id.
    readOnly: true
  internal_product_id:
    type: string
    description: Trường `internal_product_id`.
    readOnly: true
  provider_product_id:
    type: string
    description: Trường `provider_product_id`.
    readOnly: true
  provider_id:
    type: string
    description: Trường `provider_id`.
    readOnly: true
  routing_weight:
    type: integer
    description: Trường `routing_weight`.
  is_active:
    type: boolean
    description: Trường `is_active`.
  notes:
    type: string
    description: Trường `notes`.
  created_at:
    type: string
    description: Ngày tạo.
    readOnly: true
  provider:
    type: object
    properties:
      code:
        type: string
        description: Mã số.
      name:
        type: string
        description: Tên.
      is_enabled:
        type: boolean
        description: Trường `is_enabled`.
    description: Trường `provider`.
  provider_product:
    type: object
    properties:
      provider_product_code:
        type: string
        description: Trường `provider_product_code`.
      product_name:
        type: string
        description: Trường `product_name`.
      product_type:
        type: string
        description: Trường `product_type`.
      cert_type:
        type: string
        description: Trường `cert_type`.
      is_enabled:
        type: boolean
        description: Trường `is_enabled`.
    description: Trường `provider_product`.
  routing_counter:
    type: object
    properties:
      served_count:
        type: integer
        description: Trường `served_count`.
      current_weight:
        type: integer
        description: Trường `current_weight`.
      last_reset_at:
        type: string
        description: Trường `last_reset_at`.
    description: Trường `routing_counter`.
description: Schema `ListInternalProductMappingsDataList`.
