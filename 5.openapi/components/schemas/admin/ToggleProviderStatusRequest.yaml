type: object
example:
  is_enabled: true
  reason: Tạm ngưng do sự cố API Daousign
properties:
  is_enabled:
    type: boolean
    description: Trạng thái mới. true = bật provider, false = tắt provider.
    example: true
  reason:
    type: string
    description: Lý do toggle (ghi vào audit log để truy vết). Tối đa 500 ký tự.
    example: Tạm ngưng do sự cố API Daousign
required:
  - is_enabled
description: Schema `ToggleProviderStatusRequest`.
