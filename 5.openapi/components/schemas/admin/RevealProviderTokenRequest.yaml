type: object
example:
  admin_password: <mật khẩu admin hiện tại>
  env: PRODUCTION
properties:
  admin_password:
    type: string
    description: Mật khẩu admin hiện tại (plaintext qua HTTPS). Verify bằng 
      password_verify(). Sai → 401 (5407).
    example: <mật khẩu admin hiện tại>
  env:
    type: string
    description: 'Môi trường token cần reveal: SANDBOX hoặc PRODUCTION.'
    example: PRODUCTION
required:
  - admin_password
  - env
description: Schema `RevealProviderTokenRequest`.
