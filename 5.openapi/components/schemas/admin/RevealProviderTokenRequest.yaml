type: object
example:
  admin_password: <mật khẩu admin hiện tại>
  env: PRODUCTION
properties:
  admin_password:
    type: string
    example: <mật khẩu admin hiện tại>
    description: Trường `admin_password`.
  env:
    type: string
    example: PRODUCTION
    description: Trường `env`.
description: Schema `RevealProviderTokenRequest`.
