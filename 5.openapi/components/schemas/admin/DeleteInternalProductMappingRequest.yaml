type: object
example:
  reason: Hủy mapping vì provider không còn cung cấp product này
properties:
  reason:
    type: string
    example: Hủy mapping vì provider không còn cung cấp product này
    maximum: 500
    description: Lý do.
description: Schema `DeleteInternalProductMappingRequest`.
