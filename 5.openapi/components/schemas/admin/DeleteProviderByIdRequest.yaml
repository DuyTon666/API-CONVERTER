type: object
example:
  reason: Provider ngừng hợp tác — chuyển sang đối tác khác
properties:
  reason:
    type: string
    example: Provider ngừng hợp tác — chuyển sang đối tác khác
    description: Lý do thực hiện thao tác.
description: Schema `DeleteProviderByIdRequest`.
