type: object
properties:
  id:
    type: integer
    description: Mã id.
    readOnly: true
  code:
    type: string
    description: Mã số.
  is_enabled:
    type: boolean
    description: Trường `is_enabled`.
  deleted_at:
    type: string
    description: Ngày xóa.
  deleted_by:
    type: string
    description: Bị xóa bởi.
  reason:
    type: string
    description: Lý do.
description: Schema `DeleteInternalProductData`.
