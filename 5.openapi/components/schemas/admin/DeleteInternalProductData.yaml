type: object
properties:
  id:
    type: integer
    description: Định danh của bản ghi.
    readOnly: true
  code:
    type: string
    description: Mã định danh.
  is_enabled:
    type: boolean
    description: Trường `is_enabled`.
  deleted_at:
    type: string
    description: Thời điểm xóa bản ghi.
  deleted_by:
    type: string
    description: Người xóa bản ghi.
  reason:
    type: string
    description: Lý do thực hiện thao tác.
description: Schema `DeleteInternalProductData`.
