type: object
example:
  routing_weight: 3
  is_active: true
  notes: Tăng weight Daousign vì price tốt hơn
properties:
  routing_weight:
    type: integer
    example: 3
  is_active:
    type: boolean
    example: true
  notes:
    type: string
    example: Tăng weight Daousign vì price tốt hơn
