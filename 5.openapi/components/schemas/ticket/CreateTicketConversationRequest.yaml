type: object
properties:
  descriptions:
    type: string
    description: Nội dung yêu cầu- Tối đa 4000 ký tự
  files:
    type: array
    description: 'Tệp đính kèm- Tối đa 3 files- Tối đa 10MB mỗi file- Chỉ chấp nhận
      định dạng: .gif, .jpg, .jpeg, .png, .svg, .bmp, .pdf, .zip, .rar'
    items:
      type: string
      format: binary
required:
  - descriptions
description: Schema `CreateTicketConversationRequest`.
