type: object
properties:
  id:
    type: integer
    description: Id của phản hồi
    readOnly: true
  descriptions:
    type: string
    description: Nội dung yêu cầu hỗ trợ
  files:
    type: array
    items:
      type: object
      properties:
        url:
          type: string
          description: Đường dẫn đến file
        category:
          type: string
          description: 'Phân loại:image:Các file ảnhvideo: Các file videodocument:
            Tài liệu (PDF, Word, Excel, …)archive: File nénother: Loại khác'
    description: Thông tin các tệp tin (xem thêm bên dưới)
  owner:
    type:
      - object
      - 'null'
    properties:
      id:
        type: integer
        description: Id người tạo
        readOnly: true
      name:
        type: string
        description: Tên người tạo
      mgroup:
        type: integer
        description: 'Phân loại người dùng:   1 - Khách hàng PA   2 - Đại lý, tài
          khoản trả trước   3 - Nhân viên   4 - Khách hàng đại lý'
    description: Thông tin người tạo (xem thêm bên dưới)
description: Schema `CreateTicketConversationData`.
