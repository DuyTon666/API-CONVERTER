type: object
properties:
  ratings:
    type: array
    description: Danh sách các đánh giáTham khảo bên dưới
    items:
      type: object
      properties:
        supporter_id:
          type: integer
          description: ID nhân viên hỗ trợ
        stars:
          type: integer
          description: 'Số sao- Tối thiểu: 1- Tối đa: 5'
        descriptions:
          type: string
          description: Nội dung đánh giá- Tối đa 4000 ký tự- Bắt buộc nếu stars 
            < 3
      required:
        - supporter_id
        - stars
required:
  - ratings
description: Schema `CreateTicketRatingRequest`.
