post:
  summary: Đồng bộ trạng thái đơn hàng theo ID
  operationId: syncOrderStatus
  x-side-effects:
    schema_version: '1.1'
    meta:
      detected_by: rule_based
      confidence: 0.8
      review_status: pending
      last_updated: '2026-07-14'
    effects:
    - type: async_job
      trigger: on_success
      target:
        entity: null
      description: Sync job được trigger
      confirmed: false
      effect_id: order_sync_async_job_001
      source: rule_based
      evidence:
        source: module
        matched_keyword: sync
        rule_id: async_keywords.sync
        resource_source: module
        template_index: 0
    review:
      required: true
      reason: Side effects được suy luận từ action endpoint, chưa được xác nhận từ source document.
      note: null
  description: Đồng bộ hóa trạng thái của đơn hàng với các hệ thống liên quan dựa trên ID được cung cấp. Yêu cầu đơn hàng phải tồn tại và người dùng có quyền cập nhật trạng thái trước khi thực hiện đồng bộ.
  tags:
  - Order
  security:
  - bearerAuth: []
  x-permission: ''
  parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
    description: Tham số đường dẫn 'id'.
  responses:
    '200':
      description: Thành công
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/common/StandardSuccess.yaml
          example:
            success: true
            data: null
            error: null
    '401':
      $ref: ../../components/responses/Unauthorized.yaml
    '403':
      $ref: ../../components/responses/Forbidden.yaml
    '404':
      $ref: ../../components/responses/NotFound.yaml
    '500':
      $ref: ../../components/responses/InternalError.yaml
    '400':
      $ref: ../../components/responses/BadRequest.yaml
    '422':
      $ref: ../../components/responses/StandardError422.yaml
    '409':
      $ref: ../../components/responses/Conflict.yaml
    '429':
      $ref: ../../components/responses/TooManyRequests.yaml
    '502':
      $ref: ../../components/responses/BadGateway.yaml
