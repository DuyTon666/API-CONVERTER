type: object
properties:
  id:
    type: integer
    description: Định danh của bản ghi.
    readOnly: true
  order_number:
    type: string
    description: Trường `order_number`.
  customer_id:
    type: string
    description: Trường `customer_id`.
    readOnly: true
  internal_product_code:
    type: string
    description: Trường `internal_product_code`.
  product_type:
    type: string
    description: Trường `product_type`.
  cert_type:
    type: string
    description: Trường `cert_type`.
  years:
    type: integer
    description: Trường `years`.
  subscription_start_date:
    type: string
    description: Trường `subscription_start_date`.
  subscription_end_date:
    type: string
    description: Trường `subscription_end_date`.
  primary_domain:
    type: string
    description: Trường `primary_domain`.
  san_list:
    type: array
    items:
      type: object
    description: Trường `san_list`.
  dcv_method:
    type: string
    description: Trường `dcv_method`.
  status:
    type: string
    description: Trạng thái xử lý.
  order_type:
    type: string
    description: Trường `order_type`.
  parent_order_id:
    type: integer
    description: Trường `parent_order_id`.
  selected_provider:
    type: object
    properties:
      provider_id:
        type: integer
        description: Trường `provider_id`.
        readOnly: true
      code:
        type: string
        description: Mã định danh.
      provider_product_id:
        type: integer
        description: Trường `provider_product_id`.
        readOnly: true
      mapping_id:
        type: integer
        description: Trường `mapping_id`.
    description: Trường `selected_provider`.
  created_at:
    type: string
    description: Thời điểm tạo bản ghi.
    readOnly: true
description: Schema `RenewOrderDataRenewal_order`.
