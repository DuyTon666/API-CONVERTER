type: object
example:
  reason: Sản phẩm ngừng bán — thay bằng SKU mới
properties:
  reason:
    type: string
    example: Sản phẩm ngừng bán — thay bằng SKU mới
    description: Lý do thực hiện thao tác.
description: Schema `DeleteInternalProductRequest`.
