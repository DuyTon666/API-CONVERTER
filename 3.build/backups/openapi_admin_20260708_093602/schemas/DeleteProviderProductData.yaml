type: object
properties:
  id:
    type: string
    description: ID sản phẩm đã xóa
    readOnly: true
  provider_id:
    type: string
    description: FK providers.id
    readOnly: true
  provider_code:
    type: string
    description: Mã provider (reference debug)
  provider_product_code:
    type: string
    description: Mã sản phẩm tại provider (để reference)
  is_enabled:
    type: boolean
    description: Luôn false sau khi xóa
  deleted_at:
    type: string
    description: Timestamp soft delete (UTC)
  deleted_by:
    type: string
    description: Admin ID thực hiện xóa
  reason:
    type: string
    description: Lý do xóa (nếu có trong request)
description: Schema `DeleteProviderProductData`.
