type: object
example:
  reason: Sản phẩm ngừng bán — provider đã discontinue
properties:
  reason:
    type: string
    example: Sản phẩm ngừng bán — provider đã discontinue
    description: Lý do thực hiện thao tác.
description: Schema `DeleteProviderProductRequest`.
